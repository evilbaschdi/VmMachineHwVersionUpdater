<controls:MetroWindow x:Class="VmMachineHwVersionUpdater.MainWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
                      Title="VMware Machine Hardware Version Updater"
                      Width="1000" Height="700" EnableDWMDropShadow="True" Icon="b.ico"
                      GlowBrush="{DynamicResource AccentColorBrush}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Icons.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <controls:MetroWindow.RightWindowCommands>
        <controls:WindowCommands>
            <Button Name="Settings" Click="SettingsClick">
                <StackPanel Orientation="Horizontal">
                <Rectangle Width="20" Height="20"
                   Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}">
                    <Rectangle.OpacityMask>
                        <VisualBrush Stretch="Fill"
                         Visual="{StaticResource appbar_settings}" />
                    </Rectangle.OpacityMask>
                </Rectangle>

                <TextBlock Margin="5 0 0 0" VerticalAlignment="Center" Text="Settings" />
                </StackPanel>
            </Button>
        </controls:WindowCommands>
    </controls:MetroWindow.RightWindowCommands>
    <Grid>
        <DataGrid Name="VmDataGrid" CanUserAddRows="False" SelectionUnit="FullRow" ColumnWidth="*" AutoGenerateColumns="False"
                  GridLinesVisibility="Horizontal" MouseDoubleClick="VmDataGridOnMouseDoubleClick" SelectionChanged="VmDataGridSelectionChanged" CanUserDeleteRows="False" Margin="10,10,10,10">
        <DataGrid.Columns>
                <DataGridTextColumn Header="Display Name" Binding="{Binding DisplayName}" Width="250" IsReadOnly="True" />
                <DataGridTextColumn Header="Path" Binding="{Binding Path}" IsReadOnly="True" />
                <DataGridTextColumn Header="Guest OS" Binding="{Binding GuestOs}" Width="175" IsReadOnly="True" />
                <controls:DataGridNumericUpDownColumn Header="Version" Binding="{Binding HwVersion, UpdateSourceTrigger=PropertyChanged}" Minimum="1" Width="100" IsReadOnly="False" />
            </DataGrid.Columns>
            <DataGrid.ContextMenu>
                <ContextMenu Name="MovieContextMenu" StaysOpen="True">
                    <MenuItem Name="EditMenuItem" Header="Start..." Click="StartClick" />
                    <MenuItem Name="DeleteMenuItem" Header="Go to path..." Click="GoToClick" />
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
    </Grid>

    <controls:MetroWindow.Flyouts>
        <controls:FlyoutsControl>
            <controls:Flyout AnimateOpacity="True" AnimateOnPositionChange="True" Header="Settings" Position="Right" Width="500">
                <StackPanel Orientation="Horizontal">
                    <!--Label-->
                    <Label Content="Path" HorizontalAlignment="Left" Margin="10,0,0,0" VerticalAlignment="Top" />

                    <!--TextBox-->
                    <TextBox Name="VmPath" Margin="5,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Width="350" IsReadOnly="True" />

                    <!--Button-->
                    <Button Name="Browse" Click="BrowseClick" Content="Browse" Width="75" Margin="10,0,0,0" VerticalAlignment="Top" HorizontalAlignment="Right" />
                </StackPanel>
            </controls:Flyout>
        </controls:FlyoutsControl>
    </controls:MetroWindow.Flyouts>
</controls:MetroWindow>